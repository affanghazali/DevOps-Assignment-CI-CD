version: 0.2

env:
  variables:
    ENV_NAME: dev

phases:
  build:
    commands: -echo Entered the deploy phase...
      - echo Build started on `date`
      - echo Deploying to $ENV_NAME environment...
